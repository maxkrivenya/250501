Кривени 250501
Лабораторная работа 7
Вариант
Условие:


		Алгоритм по шагам функции void strsort(char** p, int n)
1. Начало
2. Локальные переменные:
char* holder - временный держатель строки при сортировке

3. Цикл по строкам с начала до конца счётчика i
4. Цикл по строкам с конца до И счётчика j
5. Если длина строки j-1 больше длины строки j: 
а) выделить под holder память размером строки j
б) скопировать строку j в holder
в) перевыделить память под j размером в длину строки j-1
г) скопировать строку  j-1 в строку j
д) перевыделить память под строку j-1 по размеру holder
е) скопировать holder в строку j-1
j) освободить память holder

6. Конец